# Informe de Actualización — V F2 0008
**Fecha:** 2026-07-19

## Solicitud del usuario
Continuar con el ítem 5 del checklist de comercialización: metas de ahorro (meta con monto objetivo, fecha opcional, barra de progreso, aportes manuales).

## Análisis previo al cambio
Se revisó `js/secciones/tarjetas.js` (patrón de tarjetas de crédito: tarjetas como cards con barra de progreso, cifras, inputs editables inline, modal de alta, borrado) para reutilizarlo como base — es la funcionalidad existente más parecida a lo que pide una meta de ahorro (monto acumulado vs. un límite/objetivo, con progreso visual). Se confirmó que las clases CSS `.cc-card-*` no están acopladas a "tarjetas" específicamente (son genéricas), así que se reutilizaron directo en vez de duplicar una familia de clases `.goal-*` paralela — solo se agregó una clase nueva para el estado "meta cumplida". El usuario proveyó el ícono de Cloudinary para la pestaña nueva.

## Modificaciones realizadas

### 1. Nueva colección `savingsGoals` (`js/state.js`)
- **Tipo:** INSERCIÓN
- `appState.savingsGoals = []` (estado inicial y reset en `unloadUserData`).
- Nuevo `onSnapshot` sobre `artifacts/{appId}/users/{uid}/savingsGoals` en `loadUserData`, mismo patrón que `creditCards`/`recurringExpenses`. No requirió cambios en `firestore.rules` — la regla ya cubre cualquier colección nueva bajo el usuario.

### 2. Sección Metas de Ahorro (`js/secciones/metas.js`, nuevo archivo)
- **Tipo:** INSERCIÓN
- `renderSavingsGoals()`: tarjetas ordenadas por fecha objetivo (las sin fecha al final), barra de progreso, cifras "Ahorrado"/"Restante", estado especial "🎉 ¡Meta cumplida!" cuando `savedAmount >= targetAmount`.
- Edición inline de `savedAmount`/`targetAmount` directo en la tarjeta (mismo patrón que `card-data-input` de tarjetas) — así se registran los aportes manuales, sin un historial de aportes aparte (no se pidió, se puede agregar después como funcionalidad propia si hace falta).
- Modal de alta (nombre, monto objetivo, ahorrado inicial opcional, fecha opcional) y eliminación con confirmación.

### 3. HTML/CSS (`index.html`, `css/secciones.css`)
- **Tipo:** INSERCIÓN
- Template `tpl-goal-card` reutilizando `.cc-card`/`.cc-card-header`/`.cc-card-figures`/`.cc-card-inputs` etc. — solo 3 líneas de CSS nuevas (`.goal-complete`, `.goal-complete-text`, sombra de `#meta-modal`).
- Nueva pestaña "Metas de Ahorro" en la tab-bar (ícono provisto por el usuario), botón de acción `+ Nueva Meta`, sección `#section-metas`, modal `#meta-modal`.
- Textos descriptivos de exportar JSON y eliminar cuenta actualizados para mencionar "metas de ahorro".

### 4. Backup JSON (`js/lib/exports.js`)
- **Tipo:** INSERCIÓN
- `savingsGoals` agregado a `cfgExportJson`/`cfgImportJson` (copia de seguridad completa). No se tocó la exportación a Excel (4 hojas) — no se pidió una quinta hoja.

### 5. Wiring (`js/main.js`)
- **Tipo:** INSERCIÓN
- Import, `SECTION_RENDERERS.metas`, `TAB_ACTIONS.metas`, `ALL_ACTION_IDS`, `initMetas()`, `"meta-modal"` en `initModalSystem`.

### 6. Bump de `CACHE_NAME` (`sw.js`)
- **Tipo:** REEMPLAZO
- `'foresee-cache-v9'` → `'foresee-cache-v10'`.

## Verificación
- `node --check` sobre los 4 módulos `.js` tocados/nuevos — sin errores.
- Playwright headless (mock de `appState.savingsGoals`, sin login real), desktop y mobile (390px): confirmado el orden por fecha, colores de progreso, estado "cumplida", cifras formateadas, y el modal de alta. Sin errores de consola.

## Estado final
Corregido y verificado. El ítem 5 del plan de comercialización queda completo — primera pestaña nueva agregada desde la reconstrucción original.
