# Informe de Actualización — V F2 0010
**Fecha:** 2026-07-19

## Solicitud del usuario
Revisar qué exporta e importa exactamente el backup JSON de Configuración, y completar lo que faltara.

## Análisis previo al cambio
Se revisó `js/lib/exports.js` completo y se comparó contra todos los campos de `appState` (`js/state.js`). El export/import ya estaba simétrico entre sí (11 campos, todos los que exportaba los sabía importar de vuelta), pero comparado contra el estado real de la app faltaban dos campos que sí son datos reales del usuario: `gastosComunes` (personas/ítems de Gastos Comunes) y `currency` (moneda configurada). El resto de los campos ausentes (`lastMonthProcessed`, `notificationsEnabled`, `filterMonth`, `currentTab`, etc.) están correctamente excluidos a propósito — son estado de sesión/UI, no datos para respaldar.

## Modificaciones realizadas

### 1. `gastosComunes` y `currency` en el backup (`js/lib/exports.js`)
- **Tipo:** INSERCIÓN
- Agregados a `cfgExportJson`.
- `currency` se suma a `prefData` en `cfgImportJson` (mismo patrón que `categoryBudgets`/`userAlias`, se guarda en el documento de preferencias).
- `gastosComunes` se restaura aparte con su propio `setDoc` — a diferencia de las demás colecciones (`transactions`, `creditCards`, etc.), `gastosComunes` es un documento único (`DB_GASTOS_COMUNES`), no una colección, así que no encaja en el helper `importCol` existente.

### 2. Texto descriptivo (`index.html`)
- **Tipo:** REEMPLAZO
- El texto de Configuración → Exportar ahora menciona "gastos comunes" entre lo que incluye el JSON.

### 3. Bump de `CACHE_NAME` (`sw.js`)
- **Tipo:** REEMPLAZO
- `'foresee-cache-v10'` → `'foresee-cache-v11'` — se tocó `.js`.

## Verificación
- `node --check js/lib/exports.js` — sin errores.
- Playwright headless: se interceptó `URL.createObjectURL` para leer el contenido real del Blob exportado (sin depender de la descarga del archivo) — confirmado que `currency` y `gastosComunes` quedan en el JSON con los valores simulados correctos.

## Estado final
Corregido y verificado. El backup/restore de Configuración ahora cubre todos los datos reales del usuario.
